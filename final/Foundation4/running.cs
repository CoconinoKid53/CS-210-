using System;


public class running{
public string running_activity() {
        Console.WriteLine("1.when do you want to run");
        string running_date  = Console.ReadLine();
        return running_date;
    }
    public class running_length{
        Console.writeline("2.what is the running_length you want to run?");
        int calculated_running_length = Console.Read();
        return calculated_running_length();


         private running() {
                int calculated_running_length = (minutes/distance)* 60
                int running_pace_calculated = minutes / distance        }


    }

}